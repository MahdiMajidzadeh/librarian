/// BookShelfKit — headless core of Librarian: database, scanning, grouping,
/// metadata parsing, online lookup, rename engine, and export.
public enum BookShelfKit {
    public static let version = "0.1.0"
}
